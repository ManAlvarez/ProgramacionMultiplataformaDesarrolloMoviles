<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tiendavirtual">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ModificarDatosActivity"></activity>
        <activity android:name=".PedidosRechazadosActivity" />
        <activity android:name=".PedidosAceptadosActivity" />
        <activity android:name=".ComprasRealizadasActivity" />
        <activity android:name=".PedidosTramiteAdministradorActivity" />
        <activity android:name=".PedidosTramiteCienteActivity" />
        <activity android:name=".DatosPedidoActivity" />
        <activity android:name=".HacerPedidoActivity" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".ClienteActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>